# Ring Authentication
# Generate a refresh token by running: npm run auth
RING_REFRESH_TOKEN=

# Optional: Restrict to specific location IDs (comma-separated)
# RING_LOCATION_IDS=

# Optional: Camera polling interval in seconds (default: 30)
# RING_CAMERA_POLL_SECONDS=30

# Optional: Location mode polling interval in seconds
# RING_LOCATION_POLL_SECONDS=

# Optional: Enable debug logging
# RING_DEBUG=false

# Event Log Settings
# Max events to retain in memory (default: 1000)
# EVENT_LOG_MAX_SIZE=1000

# Event log file path (default: ./ring-events.log)
# EVENT_LOG_FILE=./ring-events.log

# Database Settings
# SQLite database file path (default: ./ring-data.db, use ":memory:" for no persistence)
# RING_DATABASE_PATH=./ring-data.db

# Max routine log entries to retain (default: 100000)
# ROUTINE_LOG_MAX_SIZE=100000

# Cloud event cache max age in minutes (default: 30)
# CLOUD_CACHE_MAX_AGE_MINUTES=30
